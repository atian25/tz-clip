# TZClip

TZClip 是一款为 macOS 设计的现代、高效截图工具，基于 Swift 和最新的 ScreenCaptureKit 技术构建。它旨在提供流畅的截图体验，支持智能窗口识别、丰富的标注功能以及高效的快捷键操作。

## ✨ 核心特性

### 🎯 智能选择
*   **混合模式**: 同时支持自由拖拽选区和智能窗口识别。
*   **窗口吸附**: 鼠标悬停自动高亮窗口，点击即可一键选中（包含智能容器识别）。
*   **边缘吸附**: 移动选区时自动吸附屏幕边缘，辅助精准定位。
*   **键盘微调**: 支持使用方向键像素级微调选区位置和大小。

### 🎨 强大标注
*   **丰富工具**: 提供矩形、椭圆、直线、箭头、画笔、文字等多种标注工具。
*   **自由编辑**: 所有标注均支持选中后再次移动、缩放和修改属性。
*   **属性面板**: 动态调整线条颜色、粗细 (S/M/L) 和文字样式 (加粗)。
*   **Shift 约束**: 
    *   绘制/调整矩形时锁定为正方形。
    *   绘制/调整椭圆时锁定为正圆。
    *   绘制直线/箭头时锁定 45 度角倍数。

### 🛠️ 极致交互
*   **无 Dock 模式**: 纯净的菜单栏应用，不占用 Dock 空间。
*   **悬浮工具栏**: 智能跟随选区，自动避让屏幕边缘。
*   **多屏支持**: 完美适配多显示器环境。

## 🚀 快速开始

### 环境要求
*   macOS 13.0 (Ventura) 或更高版本。
*   Xcode 15+ (Swift 5.9+)。

### 构建与运行

TZClip 提供了方便的 `Makefile` 来简化开发流程。

1.  **构建项目**:
    ```bash
    make build
    ```

2.  **运行应用**:
    ```bash
    make run
    ```
    *注意：首次运行时，系统会请求屏幕录制权限，请前往“系统设置 -> 隐私与安全性 -> 屏幕录制”中授予 TZClip 权限并重启应用。*

3.  **停止应用**:
    ```bash
    make stop
    ```

## ⌨️ 快捷键指南

| 按键 | 作用 |
| --- | --- |
| **ESC** | 取消当前选区 / 退出截图 |
| **Cmd + Q** | 退出应用 |
| **Cmd + Z** | 撤销上一步标注 |
| **Delete / Backspace** | 删除选中的标注 |
| **Shift + 拖拽** | 锁定宽高比 / 角度 |
| **方向键** | 移动选区 (1px) |
| **Shift + 方向键** | 移动选区 (10px) |
| **Option + 方向键** | 调整选区大小 |

## 📁 项目结构

```
TZClip/
├── Sources/TZClip/
│   ├── main.swift              # 应用入口
│   ├── AppDelegate.swift       # 生命周期管理
│   ├── SelectionView.swift     # 核心交互视图
│   ├── AnnotationOverlayView.swift # 标注绘制层
│   ├── WindowInfoProvider.swift    # 窗口识别服务
│   └── ...
├── docs/                       # 设计文档与 RFC
└── Makefile                    # 构建脚本
```

## 📄 文档

*   [架构设计 (Architecture)](docs/architecture.md)
*   [开发计划 (Plan)](docs/plan.md)
*   [RFC 001: 核心截图流程](docs/rfcs/001_core_capture_flow.md)
*   [RFC 002: 选区交互优化](docs/rfcs/002_selection_interaction.md)
*   [RFC 003: 标注工具栏](docs/rfcs/003_toolbar_and_annotation.md)
*   [RFC 005: Pin 图功能](docs/rfcs/005_pin_feature.md)
